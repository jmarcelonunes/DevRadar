// Métodos HTTP: GET, POST, PUT, DELETE

//Tipos de parâmetros:
//Query Params: request.query (Filtros, ordenação, paginação ...)
//Route Params: request.params (Identificar um recurso na alteração ou remoção)
//Body Params: request.body (Dados para uma criação ou alteração de um registro)

//MongoDB (Não-relacional)


//funcoes do controller
// index(mostrar lista de recurso), show(mostrar um unico recurso), store(salvar), update(alterar), destroy(deletar)